//! Everything Codex: the app-server agent adapter, CLI distribution, auth, and
//! rollout history (for resuming native terminal sessions).

pub mod agent;
pub mod auth;
pub mod download;
pub mod history;
