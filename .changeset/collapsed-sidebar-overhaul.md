---
"warden": patch
---

Collapsed sidebar overhaul: groups in the collapsed rail now show a hover popover listing their folders (click one to open that folder's view), the update banner now covers Warden app updates only (agent CLI updates already live in the titlebar) and shrinks to an icon button whose popover holds the update details, and the Settings row collapses cleanly to an icon. The empty "no groups" state is now a proper card with a New group button (an icon button in the collapsed rail). Sidebar resizing has been removed; the sidebar is a fixed width. The quick-switcher popover on the titlebar's collapse button is retired, since the collapsed rail now covers that navigation.
