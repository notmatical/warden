---
"warden": minor
---

The composer's mode selector now has a tooltip (matching the model one) showing the Shift+Tab cycle shortcut. The context-meter popover is more legible (larger numbers, clearer hierarchy) and token counts use a cleaner compact format — `1M`, `1.5k`, `1.25k` instead of `1.00M`/`1.5k`. Removed an unused duplicate formatter module.
