---
"warden": minor
---

Zed-style diff tab: the session Diff button now opens a real tab showing every changed file stacked as collapsible sections — sticky file headers with +/− counts, chevron to fold a file once you've reviewed it, syntax-highlighted side content rendered off-thread by `@pierre/diffs`, plus the branch's commit list. Untracked (brand-new) files now show up in the diff too. Replaces the one-file-at-a-time diff dialog.
