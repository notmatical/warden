---
"warden": patch
---

Add a global command palette (Cmd/Ctrl+K): jump to sessions and folders, start agent or terminal sessions in any folder, send Linear issues to an agent, review PRs, switch theme, and run keybound commands from one keyboard-first surface.
