---
"warden": patch
---

The new-session menu's "Agent session" item now shows the brand mark of the provider you're signed into, gently rotating through them when more than one is enabled (e.g. Anthropic ⇄ OpenAI). The provider→icon mapping is now shared across the model picker, provider settings, and this menu instead of being duplicated.
