---
"warden": patch
---

Clean up the worktree commands dialog: Setup and Teardown are now stacked, labeled sections instead of a tab group, the run-order explanation moved into an info callout, and the variable chips use the secondary badge style.
