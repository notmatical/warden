---
"warden": minor
---

Hovering a PR badge now shows a live hover card with the PR's state, review decision, diff stats, and per-check GitHub Actions results (with durations and links), in the session header and the folder view.
