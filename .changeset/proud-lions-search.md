---
"warden": patch
---

Add OpenCode as a third agent provider: agent sessions run over the local `opencode serve` HTTP API with live text streaming, tool-call rendering, and interactive permission/question prompts; native OpenCode terminals resume their exact conversation; plus managed CLI install/updates, auth status, and OpenCode Zen models in the picker.
