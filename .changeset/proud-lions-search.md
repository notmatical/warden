---
"warden": patch
---

Add OpenCode as a third agent provider: agent sessions run over the local `opencode serve` HTTP API with live text streaming and tool-call rendering, plus native OpenCode terminal sessions, managed CLI install/updates, auth status, and OpenCode Zen models in the picker.
