---
"warden": minor
---

Linux support: deb/rpm/AppImage packaging with a distro-portable AppImage launcher, WebKitGTK crash workarounds at startup, terminal-emulator detection for "open in terminal", and a Linux leg in the release pipeline with auto-update for AppImage installs.
