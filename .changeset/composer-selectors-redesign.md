---
"@warden/desktop": patch
---

The composer's model, mode, and effort pickers have a cleaner, more consistent look. Fast mode now appears as an inline toggle on the models that support it, rather than a separate switch at the bottom of the model picker.
