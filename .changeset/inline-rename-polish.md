---
"warden": patch
---

Polished the sidebar inline rename (double-click a group or session): the field now matches the row's height and shape so the row no longer jumps into a smaller box, and replaces the heavy focus ring with a subtle filled field and a hairline focus outline.
