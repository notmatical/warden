---
"@warden/desktop": patch
---

Add an in-app MCP server (`warden mcp`) so agents can act on connected integrations during a session: create, comment on, and set the status of Linear issues, and comment on or open/close GitHub issues. On by default when Linear or the GitHub CLI is available, with a toggle on the Linear settings row.
