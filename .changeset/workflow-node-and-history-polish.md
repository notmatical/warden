---
"warden": patch
---

Workflow node and run-history polish: a running node now shows a braille spinner in its header instead of a "Running… double-click to open" line and a status badge, the config form is tighter (smaller textarea, less padding) to keep nodes compact, and run history is a read-only list of the last 5 runs with their status and timestamps — viewing a past run on the canvas is gone.
