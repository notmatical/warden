---
"warden": patch
---

Workflows now flow left-to-right instead of top-to-bottom: node handles move to the left (in) and right (out) edges, new nodes lay out horizontally, and tall cards sit side by side instead of stacking and overlapping. A "Tidy layout" action arranges an existing workflow into clean dependency columns and refits the view.
