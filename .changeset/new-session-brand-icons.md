---
"warden": patch
---

The new-session menu now uses brand marks instead of generic icons: Native Claude shows the Claude logo, Native Codex the Codex logo, and "Review a PR…" the GitHub mark — matching the icons used in tabs and the model picker.
